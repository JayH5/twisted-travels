using UnityEngine;
using System.Collections;

/// <summary>
/// Simple enum for specifying the direction of a rotation.
/// </summary>
public enum RotationDirection
{
	Anticlockwise = 90, Clockwise = -90, None = 0
}
